Expand a Taskmaster task into subtasks based on complexity analysis.

Usage: `/expand <task-id> [tag]`

Example: `/expand 11 fluvial-v1`

This uses complexity reports to determine appropriate subtask count.

Execute:
```bash
task-master expand --id=<id> --tag <tag>
```
