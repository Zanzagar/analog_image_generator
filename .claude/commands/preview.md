Render a preview image using the CLI.

Usage: `/preview [style] [seed]`

Styles: `meandering`, `braided`, `anastomosing`, `stacked`

Execute:
```bash
python scripts/render_preview.py --style <style> --seed <seed>
```

For interactive preview with UI controls, use the notebook:
`notebooks/v20a_interactive_rebuild.ipynb`
